import { AutofocusDirective } from './autofocus.directive';

describe('Directive: Autofocus', () => {
  it('should create an instance', () => {
    const directive = new AutofocusDirective(<any>{});
    expect(directive).toBeTruthy();
  });
});
